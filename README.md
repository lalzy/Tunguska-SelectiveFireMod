# Tunguska-SelectiveFireMod
Source code for mySelectiveFire mod (unecessary after dev implemented it, exist for legacy pruposes).

Since it was implemented by the dev, It is no longer available on nexusmods.
